# Project-wide Gradle settings.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the Gradle Daemon.
# Setting a larger heap size helps with OutOfMemoryError during build.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX libraries use this property to enable Jetifier.
# It's recommended to keep it enabled if you use libraries that haven't migrated to AndroidX.
android.useAndroidX=true
# android.enableJetifier=true

# Enables Kotlin incremental compilation
kotlin.incremental=true

# Enables parallel execution of tasks
org.gradle.parallel=true

# Caching for builds
org.gradle.caching=true